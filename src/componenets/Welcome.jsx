const Welcome = ({ name }) => {
    return (
        <div>
            <h1>Hello, {name}!</h1>
            <p>Welcome to the React Testing Library course.</p>
        </div>
    );
};

export default Welcome;